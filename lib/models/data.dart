class Data {
  String mail;
  DateTime deadline;
  String message;
  bool locked;

  Data({
    required this.mail,
    required this.deadline,
    required this.message,
    required this.locked,
  });
}
